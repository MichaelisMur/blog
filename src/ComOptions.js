import React from 'react';

const ComOptions = (props)=>{
    // if(props.visible){
        return(
            <h1 className="comOptions">
                WOOOW
            </h1>
        )
    // } else {
        // return(
        //     <div className="comOptions"></div>
        // )
    // }
}

export default ComOptions;